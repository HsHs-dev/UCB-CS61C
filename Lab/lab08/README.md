to run Camera:

```bash
java -jar Camera.jar
```

to complete exercise 4, download openjdk-8-jdk

```bash
sudo apt install openjdk-8-jdk
```

then run the `appletviewer` explicitly from the opnejdk-8-jdk folder

```bash
usr/lib/jvm/java-8-openjdk-amd64/bin/appletviewer https://denninginstitute.com/workbenches/vmsim/vm.html
```

