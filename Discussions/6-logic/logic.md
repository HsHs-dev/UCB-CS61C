# Logic, SDS, FSM

## 1 Pre-Check

1.1: False, simpler boolean logic makes smaller hardware which affects the latency making it faster

<!-- 1.2: True, same as 1 -->
1.2: False. Propagation delays add with the depth of the circuit, so a wide circuit with
more gates can have less delay than just a few gates arranged in sequence.

1.3: False. This can result in instability if flip flops are connected to each other

## 2 Boolean Logic
