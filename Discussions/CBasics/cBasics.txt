1.1: True
1.2: A pointer is a special variable that holds the address of other variable in memory; an array variable is, in fact, a pointer to the first element of the array
1.3: The variable will be implicitly casted to a pointer; segmentation fault
1.4: If you need to keep access to data over several function calls, use the heap. If you’re
    dealing with a large piece of data, passing around a pointer to something on the heap
    is more efficient and a better practice than passing around the data itself. (Think:
    carrying a library around vs knowing the address TO the library). Heaps grow up
    and stacks grow down, meeting when working memory is full.

2.1: pp: 0xF9320904, *pp: 0xF93209AC, **pp: 0x2A
2.2:
    (a) returns the sum of the first N elements of arr
    (b) returns -1 times the number of zeroes in the first N elements of arr
    (c) try to swap the numbers x and y, but doesn't (pass by value)
    (d) xor then not the result, or just x == y

3.1:
  (a) void swap(int* x, int* y) {
        int temp = *x;
        *x = *y;
        *y = temp;
      }

  (b) int mystrlen(char* str) {
        int size = 0;
        while (*str++ != '\0')
          size++
        return size;
      }

3.2:
  (a) had to pass the size of the array alongside with it's pointer
  (b) had to put parentheses to force operator precedence, dereference then increment + to know where the string ends had to check for '\0'
  (c) no errors, just a warning about wrapping the condition in parentheses
  (d) replaceptr is not a pointer, it should be char *srcptr, *replaceptr

4.1:
